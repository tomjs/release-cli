export const isDev = process.env.NODE_ENV === 'development';

export const NPM_REGISTRY = 'https://registry.npmjs.org';
export const NPM_YARN_REGISTRY = 'https://registry.yarnpkg.com';

export const GIT_REPO_HOSTS = ['github.com', 'gitee.com'];
